#include "Keyboard.h"
#include "utils.h"
/*
Keyboard::Keyboard(ALLEGRO_EVENT &_event)
{
 al_install_keyboard();
 event = _event;
}


Keyboard::~Keyboard()
{

}

bool Keyboard::getKey(char _key)
{
 if(event.type==ALLEGRO_EVENT_KEY_CHAR)
 {
  if(Btwn('!', _key, '`'))  return (event.keyboard.unichar == _key);
  else                      return (event.keyboard.keycode == _key);
 }
}

void Keyboard::update()
{

 
 }*/