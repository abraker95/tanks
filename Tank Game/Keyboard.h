#pragma once
#include <SFML/Graphics.hpp>

/*
class Keyboard
{
 public:
    Keyboard(ALLEGRO_EVENT &_event);
    virtual ~Keyboard();

    bool getKey(char _key);

 private:
     void update(); // ?

     char scanCodes[256]; // ?
     ALLEGRO_EVENT event;
};
*/
